<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <artifactId>date-plugin</artifactId>
    <groupId>rubberbandage.teamcity</groupId>
    <version>1.3-SNAPSHOT</version>
  </parent>
  <artifactId>build</artifactId>
  <packaging>pom</packaging>
  <properties>
      <maven.build.timestamp.format>yyyyMMddHHmmss</maven.build.timestamp.format>
      <teamcity.date-plugin.plugin.version>snapshot-${maven.build.timestamp}</teamcity.date-plugin.plugin.version>
      <teamcity.date-plugin.plugin.vendorName>Jacob Olsen</teamcity.date-plugin.plugin.vendorName>
      <teamcity.date-plugin.plugin.vendorUrl>https://www.linkedin.com/in/jacob-o</teamcity.date-plugin.plugin.vendorUrl>
      <teamcity.date-plugin.plugin.repoUrl>https://github.com/rubberbandage/teamcity-date-plugin</teamcity.date-plugin.plugin.repoUrl>
  </properties>
  <dependencies>
      <dependency>
           <groupId>rubberbandage.teamcity</groupId>
           <artifactId>date-plugin-server</artifactId>
           <version>1.1-SNAPSHOT</version>
      </dependency>
  </dependencies>
  <build>
    <plugins>
        <plugin>
            <groupId>com.google.code.maven-replacer-plugin</groupId>
            <artifactId>replacer</artifactId>
            <version>1.5.2</version>
            <executions>                
                <execution>
                    <phase>process-sources</phase>
                    <goals>
                        <goal>replace</goal>
                    </goals>
                </execution>
            </executions>
            <configuration>
                <file>${basedir}/../teamcity-plugin.xml</file>
                <outputFile>${basedir}/target/teamcity-plugin.xml</outputFile>
                <replacements>
                    <replacement>
                        <token>@Version@</token>
                        <value>${teamcity.date-plugin.plugin.version}</value>
                    </replacement>
                    <replacement>
                        <token>@VendorName@</token>
                        <value>${teamcity.date-plugin.plugin.vendorName}</value>
                    </replacement>
                    <replacement>
                        <token>@VendorURL@</token>
                        <value>${teamcity.date-plugin.plugin.vendorUrl}</value>
                    </replacement>
                    <replacement>
                        <token>@RepoURL@</token>
                        <value>${teamcity.date-plugin.plugin.repoUrl}</value>
                    </replacement>
                </replacements>
            </configuration>
        </plugin>
        <plugin>
            <artifactId>maven-assembly-plugin</artifactId>
            <version>2.4</version>
            <executions>
              <execution>
                <id>make-assembly</id>
                <phase>package</phase>
                <goals>
                  <goal>single</goal>
                </goals>
                <configuration>
                  <finalName>date-plugin</finalName>
                  <outputDirectory>${project.parent.build.directory}</outputDirectory>
                  <appendAssemblyId>false</appendAssemblyId>
                  <descriptors>
                    <descriptor>plugin-assembly.xml</descriptor>
                  </descriptors>
                </configuration>
              </execution>
            </executions>
        </plugin>
    </plugins>
  </build>
</project>
